import 'package:flutter/material.dart';

class AdPages extends StatefulWidget {
  @override
  AdPagesState createState() => new AdPagesState();
}

class AdPagesState extends State<AdPages> {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Scaffold(
      body: new Center(
        child: new Text("广告页"),
      ),
    );
  }
}
